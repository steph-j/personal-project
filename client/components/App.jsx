import React from 'react'
import Header from './Header'

import api from '../api'

export default React.createClass({
  render() {
    return (
      <div>
    <Header />
    <Dest />
    </div>
    )
  }
})
